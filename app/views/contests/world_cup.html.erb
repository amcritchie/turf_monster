<!-- World Cup 2026 branded header -->
<div class="text-center mb-8">
  <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-primary/10 border border-primary/20 mb-4">
    <span class="text-sm font-bold text-primary tracking-wide">FIFA WORLD CUP 2026</span>
  </div>
  <h1 class="text-3xl md:text-4xl font-extrabold text-heading">Turf Totals</h1>
  <p class="text-secondary text-sm mt-2"><%= @contest.tagline || @contest.name %></p>
</div>

<%= render "contests/turf_totals_board" %>
