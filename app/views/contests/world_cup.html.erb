<div class="text-center mb-8">
  <h1 class="text-3xl md:text-4xl font-extrabold text-heading">Build <%= @contest.picks_required %> Team Lineup</h1>
  <p class="text-secondary text-sm mt-2"><%= @contest.tagline || @contest.name %></p>
</div>

<%= render "contests/turf_totals_board" %>
