<div class="flex-1 overflow-y-auto p-4 space-y-3">
  <template x-for="i in 4" :key="i">
    <div>
      <template x-if="pickSlots[i-1]">
        <div class="bg-surface-alt rounded-xl p-3 border transition"
             :class="[pickSlots[i-1].selection === 'more' ? 'border-mint-700' : 'border-red-700', isShaking(pickSlots[i-1].propId) ? 'shake' : '']">
          <div class="flex items-center justify-between">
            <div class="flex items-center gap-2">
              <template x-if="pickSlots[i-1].emoji">
                <div class="flex items-center gap-1 text-sm">
                  <span x-text="pickSlots[i-1].emoji"></span>
                  <span class="text-[10px] text-muted font-bold">v</span>
                  <span x-text="pickSlots[i-1].opEmoji"></span>
                </div>
              </template>
              <div>
                <p class="text-heading font-semibold text-sm" x-text="pickSlots[i-1].name"></p>
                <p class="text-xs mt-0.5">
                  <span :class="pickSlots[i-1].selection === 'more' ? 'text-mint' : 'text-red-400'"
                        x-text="pickSlots[i-1].selection === 'more' ? 'OVER' : 'UNDER'"></span>
                  <span class="text-violet font-mono ml-1" x-text="pickSlots[i-1].line"></span>
                  <template x-if="pickSlots[i-1].opName">
                    <span class="text-muted ml-1">vs <span x-text="pickSlots[i-1].opName"></span></span>
                  </template>
                </p>
              </div>
            </div>
            <button @click="removePick(pickSlots[i-1].propId)"
                    class="text-muted hover:text-red-400 transition text-lg leading-none">&times;</button>
          </div>
        </div>
      </template>
      <template x-if="!pickSlots[i-1]">
        <div class="border-2 border-dashed border-strong rounded-xl p-3 text-center">
          <p class="text-muted text-sm">Pick <span x-text="i"></span></p>
        </div>
      </template>
    </div>
  </template>
</div>
